[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flask-lab4"
version = "0.1.0"
description = "Lab 4: CI/CD Best Practices"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["app"]